import { NgModule } from '@angular/core';

// TODO: Looks like we don't need such a module, in this folder we should just keep root services
@NgModule({
    providers: [],
})
export class ServicesModule {}
